# Qt Framework - Install ONE of the following:
# For modern systems (Python 3.9+): pip install PyQt6
# For older systems: pip install PySide2
# PyQt6 is preferred for standard PCs (x86_64) and modern ARM (aarch64)
PyQt6>=6.6.0; (platform_machine == "x86_64" or platform_machine == "AMD64" or platform_machine == "aarch64") and python_version >= "3.10"
# PyQt5 is a solid choice for 32-bit Windows (x86) where PyQt6 is unavailable
PyQt5; (platform_machine == "x86" or platform_machine == "i386") and python_version >= "3.6"
# PySide6 as fallback/alternative for modern systems
PySide6>=6.0.0; (platform_machine == "x86_64" or platform_machine == "AMD64" or platform_machine == "aarch64") and python_version >= "3.8" and python_version < "3.10"
# PySide2 is more stable on ARMv7 and older systems.
# We exclude i686/i386 Linux because it usually requires system packages (python3-pyqt5).
PySide2; (platform_machine == "armv7l" or (python_version < "3.8" and platform_machine != "i686" and platform_machine != "i386"))

# PDF Libraries
PyMuPDF>=1.23.0
pdfminer.six>=20221105
pikepdf>=8.0.0
Pillow>=10.0.0

# Backports for older Python versions
importlib-metadata>=6.0.0; python_version < "3.8"
